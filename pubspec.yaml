name: wasnaker_core
description: Shared foundation for Wasnaker actor apps — extends Nylo + flutter_modular.
version: 0.1.0

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  nylo_framework: ^7.1.17
  flutter_modular: ^6.4.1
  font_awesome_flutter: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
